# Copy to .env.local for local development. In production, configure this in
# the hosting provider rather than committing a .env file.
# /api is recommended when the host or reverse proxy forwards API requests to
# the backend; it preserves same-origin CSRF cookie behavior.
VITE_API_URL=/api
