# Copy this file to .env and fill in real values. Never commit .env itself.

PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string (from your Atlas cluster)
MONGO_URI=mongodb://localhost:27017/program

# Long random string used to sign JWTs. Generate one with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=8bd9ef67e1a6e2820e06b11190995e9f9b3b007afa28f91da731c9e5e12661f799de89bf67b34bc63d18e27bd08495700bfd5ab6759fc28b653a41fb370b4ef0

# How long the JWT (and cookie) stays valid
JWT_EXPIRES_IN=7d

# The exact URL of your deployed frontend (no trailing slash).
# In development this is your Vite dev server.
CLIENT_URL=http://localhost:5173,https://sat-badly-mice-consortium.trycloudflare.com

# From your Cloudinary dashboard (cloudinary.com/console) — free tier is fine for MVP


CLOUDINARY_CLOUD_NAME=iucsgpua
CLOUDINARY_API_KEY=118666173514645
CLOUDINARY_API_SECRET=SLrzf626jasLTm1YuXfJnAFXSdU

# ── Payout OTP — Gmail SMTP ───────────────────────────────────────────────────
# Create a Gmail App Password at: https://myaccount.google.com/apppasswords
# (Requires 2-Step Verification to be enabled on your Gmail account)
GMAIL_USER=ahmadnashat04@gmail.com
GMAIL_APP_PASSWORD=wvap ourk xjfx zrcg


# Secret key used to HMAC-SHA256 the OTP before storing it.
# Must be at least 32 random characters. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
OTP_SECRET=8f4b92c73d1a6e5f842b1c9d3e7a0f6b8c2d5e1f4a9b3c7d0e8f2a6b4c1d9e5f

# How long the OTP is valid (in minutes). Default: 10
OTP_EXPIRY_MINUTES=10

# Minimum seconds between OTP resend requests for the same payout. Default: 60
OTP_RESEND_COOLDOWN_SECONDS=60

# Payout amount (EGP) above which a second admin approver is required. Default: 5000
PAYOUT_APPROVAL_THRESHOLD=5000

